export type taskProps = {
  id: number;
  title: string;
  description: string;
  date: string;
  priority: string;
  boardId: string,
};

export type ProjectProps = {
  projectTitle: string;
  borderColors: string;
  boardId: string
};
